import React from "react";

const Home: React.FC = () => {
    return (
        <main className="flex flex-col w-full min-h-screen">
            HALO INI HOME PAGE
        </main>
    );
};

export default Home;