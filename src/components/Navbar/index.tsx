import React from 'react';

const Navbar: React.FC = () => {
    return (
        <div className="flex items-center justify-between bg-gray-800 p-5 px-8 sticky top-0 z-50 text-white">
            INI NAVBAR
        </div>
    )
};

export default Navbar;